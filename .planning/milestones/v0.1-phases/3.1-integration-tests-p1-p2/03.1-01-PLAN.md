# Plan 03.1-01: Mocked Leader Integration Tests

## Objective
Add mocked integration tests covering the leader's HTTP endpoints, peer push mechanism, and data API to ensure regression safety before Phase 4.

## Dependencies
- Phase 3 complete (persistence, data API, status modules exist)
- pytest-asyncio installed
- Quart test client

## Test Structure

### Coverage (19 tests, 6 classes)

| Class | Tests | Scope |
|-------|-------|-------|
| `RegistrationIntegration` | 4 | Single node registration, multi-node, re-registration idempotency, missing IP → 400 |
| `PeerPushIntegration` | 3 | Peer list POST verification, payload structure, coverage of all registered nodes |
| `SubmissionIntegration` | 4 | Valid submission → 202, invalid node_ip → 400, missing checks → 400, bad timestamp → 400 |
| `ConfigPushIntegration` | 3 | Config update → 200, state change verification, notification propagation |
| `DataApiIntegration` | 3 | 30m response structure, 30d response structure, missing window → 400 |
| `CorsIntegration` | 2 | CORS headers present on all endpoints, preflight OPTIONS handling |

### Files to create/modify
- `tests/conftest.py` — shared fixtures (app, test client, registered nodes, mock httpx)
- `tests/test_leader_integration.py` — all 6 integration test classes

## Key Fixtures (conftest.py)

```python
@pytest.fixture
def app():
    from mesh_status.leader import app
    return app

@pytest.fixture
async def client(app):
    async with app.test_client() as c:
        yield c

@pytest.fixture
def registered_nodes(client):
    # Register 3 nodes via POST /register
    ...

@pytest.fixture
def mock_httpx(mocker):
    # Mock httpx.AsyncClient to avoid real HTTP during peer push
    ...
```

## Verification
- Run `python -m pytest tests/test_leader_integration.py -v`
- All 19 tests pass
- No real HTTP calls made (httpx fully mocked)
