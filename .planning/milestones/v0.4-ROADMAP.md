# Milestone v0.4: Dashboard UI Polish

**Status:** ✅ SHIPPED 2026-06-18
**Phases:** 7a-7b
**Total Phases:** 2

## Overview

Reduced auto-refresh from 30s to 10s, added per-check-type uptime percentage inline with latency values, fixed matrix column headers to show meaningful short labels with hostname tooltips, and resolved raw HTML code rendering in expander detail cards.

## Phases

### Phase 7a: Display & Refresh Tuning

**Goal:** Improve dashboard responsiveness and info density

**Details:**
- Auto-refresh interval reduced from 30s to 10s with cache TTL adjusted to 8s
- Card shows per-check-type uptime inline with latencies (Ping: Xms (Y%), HTTP: Xms (Y%))
- Matrix column headers show short numeric labels (e.g., "1" instead of "ai") with full hostname on hover
- All 53 tests pass

### Phase 7b: HTML Rendering Fix

**Goal:** Fix raw HTML code display in expander cards

**Details:**
- Refactored `_render_detail_card` from multi-line f-string to parts-list join pattern
- Eliminates Python-Markdown parser ambiguity with block-level HTML tags
- All 53 tests pass

## Milestone Summary

**Key Decisions:**
- Split 4 v0.4 items into 2 phases: Phase 7a (items 2-4, safe display changes) and Phase 7b (item 1, investigation + fix)
- Used HTML parts-list join pattern (same as `_render_connectivity_matrix`) instead of multi-line f-strings to prevent markdown parser HTML block splitting

**Technical Debt Incurred:** None

---

_For current project status, see .planning/ROADMAP.md_
