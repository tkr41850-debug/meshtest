# Plan 03.1-02: Mocked Node Integration Tests

## Objective
Add mocked integration tests covering the node agent's check execution, semaphore-limited concurrency, buffer/retry mechanism, and registration HTTP client to ensure regression safety before Phase 4.

## Dependencies
- Plan 03.1-01 complete (conftest.py available)
- Phase 2 complete (node.py, models, config exist)
- pytest-asyncio installed

## Test Structure

### Coverage (16 tests, 5 classes)

| Class | Tests | Scope |
|-------|-------|-------|
| `CheckExecutionIntegration` | 4 | Ping success, ping failure, ping timeout, HTTP health check |
| `SemaphoreIntegration` | 3 | Verifiable concurrency cap via semaphore, concurrent peer check limiting |
| `BufferRetryIntegration` | 4 | Accumulation on submit failure, clearing on success, maxlen eviction, combined payload |
| `RegistrationHttpIntegration` | 3 | Correct POST request structure, response parsing, error handling |
| `CycleIntegration` | 2 | Full cycle: fetch peers → check → submit, cycle with buffer retry |

### Files to create/modify
- `tests/test_node_integration.py` — all 5 integration test classes

### Key Testing Patterns

```python
# Mock asyncio.create_subprocess_exec for ping
async def mock_ping(result: bool, latency: float = 10.0):
    ...

# Mock httpx.AsyncClient for HTTP checks and submission
async def mock_http_check(status: int = 200):
    ...

# Verify semaphore usage
async def test_semaphore_limits_concurrency():
    # Create 10+ peers, verify no more than 10 concurrent checks
    ...
```

## Verification
- Run `python -m pytest tests/test_node_integration.py -v`
- All 16 tests pass
- All mocks verify correct call structure (args, headers, URLs)
