---
wave: 1
id: 01-01
title: "Project Foundation & Core Server"
depends_on: []
files_modified:
  - pyproject.toml
  - mesh_status/__init__.py
  - mesh_status/__main__.py
  - mesh_status/config.py
  - mesh_status/models.py
  - mesh_status/leader.py
autonomous: true
requirements: [LEAD-01, LEAD-03, LEAD-06, LEAD-07]
---

# Plan 01-01: Project Foundation & Core Server

## Objective

Set up the Python project structure with UV, create the `mesh_status` package, implement shared models, configuration, and the Quart server scaffold with health endpoints.

## Tasks

### Task 1: Project Scaffolding

<read_first>
- .planning/phases/01-CONTEXT.md
- .planning/PROJECT.md
</read_first>

<action>
1. Create `pyproject.toml` with UV-compatible metadata:
   - Project name: `mesh-status`
   - Python >=3.12
   - Dependencies: `quart>=0.20.0`, `hypercorn>=0.18.0`, `quart-cors>=0.8.0`, `aiofiles>=24.1.0`, `httpx>=0.28.0`
   - Build system: setuptools
2. Create `mesh_status/__init__.py` (empty, just `"""mesh-status - Distributed mesh connectivity testing."""`)
3. Create `mesh_status/__main__.py` with `from mesh_status.leader import main; main()` entry point
4. Create `mesh_status/config.py` with:
   - `LEADER_PORT = 58080` (int, from env var `MESH_STATUS_PORT` with fallback)
   - `CHECK_INTERVAL = 10` (int, from env var `MESH_STATUS_INTERVAL` with fallback)
   - `PEER_PUSH_TIMEOUT = 5` (int, seconds for peer notification HTTP calls)
   - `LOG_LEVEL = "INFO"` (from env var `MESH_STATUS_LOG_LEVEL` with fallback)
5. Create `mesh_status/models.py` with dataclasses:
   - `NodeRegistration`: `node_ip: str`, optional `hostname: str`
   - `CheckResult`: `node_ip: str`, `target_ip: str`, `ping_ok: bool`, `ping_latency_ms: float | None`, `http_ok: bool`, `http_latency_ms: float | None`, `http_status: int | None`, `timestamp: float`
   - `SubmitPayload`: `node_ip: str`, `checks: list[CheckResult]`, `timestamp: float`
   - `NodeInfo`: `node_ip: str`, `hostname: str | None`, `last_seen: float`
</action>

<acceptance_criteria>
- `pyproject.toml` exists with project name `mesh-status` and all 5 dependencies listed
- `mesh_status/__init__.py` exists with docstring
- `mesh_status/__main__.py` exists and calls `main()` from leader
- `mesh_status/config.py` contains `LEADER_PORT = 58080`, `CHECK_INTERVAL = 10`, `PEER_PUSH_TIMEOUT = 5`, `LOG_LEVEL = "INFO"`
- `mesh_status/models.py` contains class `NodeRegistration`, `CheckResult`, `SubmitPayload`, `NodeInfo`
- Running `python -m mesh_status` produces an import error about missing `main` (expected — Task 2 adds it)
</acceptance_criteria>

### Task 2: Core Server with Health Endpoints

<read_first>
- mesh_status/config.py
- mesh_status/models.py
</read_first>

<action>
In `mesh_status/leader.py`:
1. Create Quart app with CORS (`quart_cors.cors(app, allow_origin="*")`)
2. Configure logging: `logging.basicConfig(level=getattr(logging, config.LOG_LEVEL), stream=sys.stderr, format="%(asctime)s [%(levelname)s] %(message)s")`
3. Initialize in-memory state:
   - `_registry: dict[str, NodeInfo]` — protected by `asyncio.Lock()`
   - `_results: dict[str, list[CheckResult]]` — `check_results` keyed by node_ip
4. Add `GET /livez` — returns `{"status": "alive"}` with 200 OK
5. Add `GET /readyz` — returns `{"status": "ready"}` with 200 OK when the app is running
6. Implement `main()` function:
   - Creates the app
   - Returns `app` for Hypercorn to run
7. In `__main__.py`, use `hypercorn.asyncio.serve()` with config on port 58080
8. Add `@app.after_request` to add CORS headers to all responses
</action>

<acceptance_criteria>
- `leader.py` contains `app = Quart(__name__)` and `cors(app, allow_origin="*")`
- `leader.py` contains `async def livez()` route returning `{"status": "alive"}`
- `leader.py` contains `async def readyz()` route returning `{"status": "ready"}`
- `leader.py` contains `_registry` and `_results` initialized in module scope
- `leader.py` contains `asyncio.Lock()` named something like `_registry_lock`
- `__main__.py` imports and calls `hypercorn.asyncio.serve()` with `Config().bind = ["0.0.0.0:58080"]`
</acceptance_criteria>

## Verification

### must_haves
- Server starts on port 58080 and responds to `GET /livez` with 200
- Server responds to `GET /readyz` with 200
- Server runs on the expected port

## Wave

1 (foundation — must complete before 01-02 and 01-03)
