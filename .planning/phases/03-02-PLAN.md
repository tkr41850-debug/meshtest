---
wave: 2
id: 03-02
title: "Data API & Status Calculation (TDD)"
depends_on: [03-01]
files_modified:
  - mesh_status/leader.py
  - mesh_status/status.py
  - tests/test_status.py
  - tests/test_data_api.py
autonomous: true
requirements: [DATA-04, DATA-05, DATA-06]
---

# Plan 03-02: Data API & Status Calculation (TDD)

## Objective

Add `GET /data` endpoints (30m and 30d windows) with dynamic node status calculation. Written test-first (TDD).

## Tasks

### Task 1: Write Status Tests (RED)

<read_first>
- .planning/phases/03-CONTEXT.md
</read_first>

<action>
Create `tests/test_status.py`:

1. Test `calculate_status(node_ip, target_ip, results, registry, now)`:
   - **OK**: Node submitted results within 3× check interval AND check passed
   - **Pending**: Node exists in registry but no results submitted yet
   - **NotAvailable**: Previous results exist but no submission within threshold
   - Per-check-type: ping_status and http_status returned independently
2. Test edge cases:
   - Freshly registered node → Pending
   - Node with old results (beyond threshold) → NotAvailable
   - Node with recent results → OK
   - Mixed: ping OK, http NotAvailable
</action>

<acceptance_criteria>
- `tests/test_status.py` exists
- Tests cover OK, Pending, NotAvailable for both check types
- Edge cases covered
</acceptance_criteria>

### Task 2: Implement Status Module (GREEN)

<read_first>
- tests/test_status.py
- mesh_status/models.py
- mesh_status/config.py
</read_first>

<action>
Create `mesh_status/status.py`:

1. `calculate_status(node_ip: str, target_ip: str, results: dict, registry: dict, now: float) -> dict`:
   - Returns `{"node_ip": node_ip, "target_ip": target_ip, "ping_status": str, "http_status": str, "last_seen": float|None}`
   - Status values: "OK", "Pending", "NotAvailable"
   - OK: results exist for this node_ip AND results are within 3×CHECK_INTERVAL
   - Pending: node_ip is in registry but NO results exist at all
   - NotAvailable: node_ip has OLD results but nothing within threshold
   - Per-check-type independence: ping_ok / http_ok from latest result
</action>

<acceptance_criteria>
- All tests from Task 1 pass
- Status calculation is accurate per check type
</acceptance_criteria>

### Task 3: Write Data API Tests (RED)

<read_first>
- mesh_status/status.py
- mesh_status/persistence.py
</read_first>

<action>
Create `tests/test_data_api.py`:

1. Test `GET /data?window=30m`:
   - Returns results from last 30 minutes
   - Includes node status per peer pair
   - Returns 200 with JSON body
2. Test `GET /data?window=30d`:
   - Returns daily aggregated uptime
   - Each day has per-peer-pair stats
3. Test `GET /data` without window param → 400
4. Test `GET /data?window=invalid` → 400
5. Test CORS headers present on response
</action>

<acceptance_criteria>
- `tests/test_data_api.py` exists
- Tests cover both window sizes, error cases, and CORS
</acceptance_criteria>

### Task 4: Add Data API Routes to Leader (GREEN)

<read_first>
- tests/test_data_api.py
- mesh_status/leader.py
- mesh_status/status.py
- mesh_status/persistence.py
</read_first>

<action>
Add to `mesh_status/leader.py`:

1. `GET /data` with query param `window`:
   - `?window=30m`: Last 30 minutes of raw checks + per-peer status
     - Read recent results from `_results` dict + recent file data
     - Calculate status for each peer pair via `status.calculate_status()`
     - Return `{"window": "30m", "checks": [...], "statuses": [...], "timestamp": now}`
   - `?window=30d`: Last 30 days of daily aggregated uptime
     - Read from date-partitioned files for last 30 days
     - Aggregate per day: for each node-pair, compute uptime % (OK checks / total checks)
     - Return `{"window": "30d", "days": [{"date": "...", "connections": [...]}]}`
   - Missing/invalid window → 400 with error
2. CORS headers already present from `@cors` decorator (covered by DATA-07)

3. Register background flush task in `@app.before_serving`:
   ```python
   @app.before_serving
   async def startup():
       asyncio.create_task(persistence.flush_loop())
   ```
</action>

<acceptance_criteria>
- All data API tests pass
- `GET /data?window=30m` returns results with statuses
- `GET /data?window=30d` returns daily aggregates
- `GET /data` without window returns 400
- CORS headers present
</acceptance_criteria>

### Task 5: Refactor

<read_first>
- tests/test_data_api.py
- tests/test_status.py
- mesh_status/leader.py
- mesh_status/status.py
</read_first>

<action>
Run full test suite:
```bash
python3 -m pytest tests/ -v
```
Review and clean up:
- Type hints
- Edge cases (empty results, no nodes registered, no data directory)
- Error messages
</action>

<acceptance_criteria>
- All tests pass
- Code is clean with type hints
- Edge cases handled
</acceptance_criteria>

## Verification

### must_haves
- GET /data?window=30m returns recent checks with statuses
- GET /data?window=30d returns daily aggregates
- Status is calculated per check type (ping/http)
- CORS headers present on all responses
