# v0.10 — Custom Hover Tooltips

**Shipped:** 2026-06-21

## Overview

Replaced native HTML `title` tooltips with custom CSS-only tooltip containers in the unified card template and matrix view, using group-based hover for bar rows. Fixed CI pipeline to install dev dependencies properly and added Makefile for local development. Fixed `total_checks` aggregation bug in hourly/daily views.

## Commit Range

```
2a86403 Phase 24: Custom Hover Tooltips + CI fixes + Makefile
714c829 fix: CI: install dev deps, use heredocs to avoid quoting issues
4e89dcd fix: sum total_checks across all time buckets in hourly/daily views
47cc006 test: add total_checks summing tests for hourly and daily views
```

## Diff Stats

28 files changed, 1466 insertions(+), 213 deletions(-)

## Requirements Satisfied

- **UXTIP-01**: Remove native `title` from bar spans, use `data-tooltip`
- **UXTIP-02**: CSS-only tooltip containers around bar rows in `card.ts`
- **UXTIP-03**: Tooltip CSS classes in `style.css` (dark bg, positioned, arrow)
- **UXTIP-04**: Matrix column headers use tooltip div instead of `title`
- **UXTIP-05**: Tests updated to use `.mesh-tooltip` / `data-tooltip`
- **UXTIP-06**: No visual regression — tooltips only on hover, no layout shift

## Files Changed

| File | Change |
|------|--------|
| `frontend/src/views/bars.ts` | `title` → `data-tooltip` on bar spans |
| `frontend/src/views/card.ts` | `.mesh-tooltip-group` wrappers around bar rows |
| `frontend/src/views/hourly.ts` | `sumHourlyChecks()`, group tooltip wrappers |
| `frontend/src/views/day30.ts` | `sumDailyChecks()`, group tooltip wrappers |
| `frontend/src/views/matrix.ts` | Column headers with `.mesh-tooltip-group` |
| `frontend/src/style.css` | `.mesh-tooltip-group`, `.mesh-tooltip`, `::after` arrow |
| `pyproject.toml` | `ruff>=0.11.0`, `pytest>=8.0.0`, `[tool.ruff]` config |
| `.github/workflows/ci.yml` | `uv sync --extra dev`, heredoc scripts, relaxed version check |
| `Makefile` | `make ci`, `make lint`, `make test`, `make build` |
| `frontend/src/__tests__/` | 6 new tests for total_checks summing |
