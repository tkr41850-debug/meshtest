# Requirements: v0.2 Containerize mesh-status

## Owner

mesh-status

## DOCK-01 — Leader+Dashboard Dockerfile

**Description:** A Dockerfile (`Dockerfile.leader`) based on `python:3.12-slim` that installs dependencies and runs both the Quart API server (port 58080) and Streamlit dashboard (port 58581) as subprocesses.

**Acceptance criteria:**
- Base image: `python:3.12-slim` (native multi-arch support)
- UV is installed and used for dependency management
- Exposes ports 58080 and 58581
- Entrypoint launches both Hypercorn (Quart) and Streamlit
- Builds for both `linux/amd64` and `linux/arm64` via `docker buildx build --platform`
- `docker build -t mesh-leader .` succeeds

---

## DOCK-02 — Node Agent Dockerfile

**Description:** A Dockerfile (`Dockerfile.node`) based on `python:3.12-slim` that installs `iputils-ping` and runs the standalone node agent.

**Acceptance criteria:**
- Base image: `python:3.12-slim` (native multi-arch support)
- `iputils-ping` installed for the system `ping` binary
- UV is installed and used for dependency management
- Entrypoint runs the node agent (`python node.py`)
- Exposes node HTTP server port
- Builds for both `linux/amd64` and `linux/arm64` via `docker buildx build --platform`
- `docker build -t mesh-node .` succeeds

---

## DOCK-03 — Docker Compose

**Description:** A `docker-compose.yml` file that orchestrates the leader+dashboard and node agent containers for local dev/test.

**Acceptance criteria:**
- `docker compose up` starts both services
- Leader is accessible on port 58080
- Dashboard is accessible on port 58581
- Node connects to leader automatically
- Data directory bind-mounted for persistence
- Environment variables configurable via compose file

---

## DOCK-04 — Docker Config

**Description:** Docker-specific configuration including environment variables, port mappings, and volume mounts.

**Acceptance criteria:**
- Leader env vars: `LEADER_HOST`, `LEADER_PORT` (default 58080)
- Node env vars: `LEADER_IP`, `NODE_IP`, `MESH_STATUS_INTERVAL`
- Dashboard env var: `LEADER_URL`
- Data volume: `/app/data` bind-mounted for persistence
- All env vars have sensible defaults

---

## DOCK-05 — Deployment Docs

**Description:** README section documenting Docker-based deployment.

**Acceptance criteria:**
- Prerequisites listed (Docker, Docker Compose)
- Quick start: `docker compose up`
- Configuration via environment variables documented
- Build instructions for individual containers (single-arch and multi-arch)
- Volume mount for data persistence documented
- Multi-arch build command documented: `docker buildx build --platform linux/amd64,linux/arm64 ...`
- Note: requires `docker buildx create --use` if not already configured

---

## DOCK-06 — GitHub Actions CI/CD

**Description:** A GitHub Actions workflow that builds both Docker images (leader+dashboard, node agent) and pushes them to Docker Hub on push/tag.

**Acceptance criteria:**
- Workflow file at `.github/workflows/docker-publish.yml`
- Builds both Dockerfile.leader and Dockerfile.node
- Uses docker buildx for multi-arch (linux/amd64, linux/arm64)
- Logs into Docker Hub using `DOCKER_USERNAME` and `DOCKER_PASSWORD` secrets
- Tags images with `latest` on main branch pushes
- Tags images with git tag version on tag pushes
- Workflow is triggered on `push` and `tag` (document if manual trigger only)
- Docker Hub org/repo created and documented
- GitHub repo secrets configured and documented
